#!/bin/sh

# this pack is valid for apps with a hello.txt in the root
# if [ -f $1/hello.txt ]; then
#   echo "HelloFramework"
#   exit 0
# else
#   exit 1
# fi


# takes BUILD_DIR as a single argument
echo "OpenCV 3.0"
# SBL exit 0 indicates that the buildback should be used, signifying no need for inference
exit 0
